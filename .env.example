# MinIO Configuration
# Leave MINIO_ROOT_PASSWORD empty to auto-generate a secure password
MINIO_ROOT_PASSWORD=

# Bucket to create (used by create-bucket.sh)
BUCKET=longhorn-backups

# Only needed for local development/testing
KUBECONFIG=../k3s-infra/kubeconfig
